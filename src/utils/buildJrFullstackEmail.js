export function buildJrFullstackEmailBody(a) {
  return `OTTERMAP — JR FULL STACK DEVELOPER QUALIFIER SUBMISSION
==========================================================
Submission ID  : ${a.submissionId}
Candidate      : ${a.name}
Email          : ${a.email || "—"}
Phone          : ${a.phone || "—"}
Submitted At   : ${a.submittedAt}
Time Used      : ${a.timeUsed}

----------------------------------------------------------
PART A — Clarifying Questions
----------------------------------------------------------
${a.partA}

----------------------------------------------------------
PART B — Implementation Plan
----------------------------------------------------------
${a.partB}

----------------------------------------------------------
PART C.1 — TypeScript Type Definition
----------------------------------------------------------
${a.partC1}

----------------------------------------------------------
PART C.2 — useSearchParams Hook Usage
----------------------------------------------------------
${a.partC2}

----------------------------------------------------------
PART C.3 — Reset Button Handler
----------------------------------------------------------
${a.partC3}

----------------------------------------------------------
PART D.1 — Edge Case
----------------------------------------------------------
${a.partD1}

----------------------------------------------------------
PART D.2 — Out of Scope
----------------------------------------------------------
${a.partD2}

----------------------------------------------------------
PART E — Async PR Update
----------------------------------------------------------
${a.partE}

==========================================================
End of submission ${a.submissionId}
`;
}
